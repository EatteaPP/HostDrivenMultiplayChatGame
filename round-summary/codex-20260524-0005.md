# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 4 `PlayerActionService` 與 `SEND_MESSAGE`。
- Key changes:
  - 新增 `PlayerActionService`，作為玩家 action 的共同入口。
  - 新增 `ChatService`，負責 `SEND_MESSAGE` 驗證、建立 `ChatMessage`、紀錄 `PlayerActionRecord`。
  - 新增 `PlayerActionController`，提供 action submit API。
  - 新增 action / chat DTO：`PlayerActionRequest`、`PlayerActionResponse`、`ChatMessageView`。
  - 新增 `ActionRejectedException` 與 API 例外處理。
  - 新增 `PlayerActionServiceTest`，驗證發言成功、等待階段拒絕、淘汰玩家拒絕、房外玩家拒絕。
- Validation:
  - 執行 `mvn -q test` 成功。
- Open items / next steps:
  - Phase 5 可開始實作 HOST message。
  - Phase 6 前尚未有 `startGame`，所以一般新建房間仍停在 `WAITING`，此時 `SEND_MESSAGE` 會被正確拒絕。
  - 本輪尚未實作 cooldown、投票、stage transition、AI 或前端。
  - 自我檢查：玩家發言必須經過 `PlayerActionService`；被淘汰玩家不可發言；規則驗證在後端；未導入 DB / Redis / 登入 / 權限系統；`MESSAGE_CREATED` 與 `STAGE_CHANGED` 未混用。
