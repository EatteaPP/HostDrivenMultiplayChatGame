# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 10 `GAME_ENDED` 與結束後身份揭露。
- Key changes:
  - 新增 `GameEndedView`，只在遊戲結束事件中揭露 `PlayerRevealView`。
  - 擴充 `GameFlowEngine`，淘汰後若 `alivePlayers <= 3` 會進入 `ENDED`。
  - `ENDED` 會設定 `roomStatus`、`currentStage`、`endedAt`，並廣播 `GAME_ENDED`、`STAGE_CHANGED`、`ROOM_STATE_UPDATED`。
  - 補充結束條件測試。
  - 補充 `ENDED` stage 沒有 availableActions、結束後不可發言的測試。
- Validation:
  - 執行 `mvn -q test` 成功。
- Open items / next steps:
  - Phase 11 可開始建立 React Lobby。
  - `GAME_ENDED` 的勝負文案目前是 MVP 版本，可在前端或規格確認後再微調。
  - 本輪尚未實作 AI 或前端。
  - 自我檢查：結束前 `RoomView` / `PlayerPublicView` 不揭露 AI/HUMAN；結束後才透過 `GameEndedView` 揭露；`ENDED` 後沒有 availableActions；未導入 DB / Redis / 登入 / 權限系統。
