# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 13 `AgentController` 與 Mock AI。
- Key changes:
  - 新增 `AgentController` interface、`AgentCoordinator`、`NoopAgentController`、`MockAgentController`。
  - 新增 `POST /api/rooms/{roomId}/ai-players`，可在等待中房間加入 AI player。
  - `RoomService` 支援建立 AI player，但 public DTO 仍不揭露 AI/HUMAN。
  - `GameFlowEngine` 在計算 availableActions 後通知 AI controller。
  - Mock AI 透過 `PlayerActionService` 送出 `SEND_MESSAGE` 或 `SUBMIT_VOTE`，不直接修改 domain state。
  - 前端 Game Room 新增 `Add AI` 按鈕。
  - 前端修正 dev server HMR 下可能重複 `createRoot` 的問題。
  - 新增 `MockAgentControllerTest`。
- Validation:
  - 執行後端 `mvn -q test` 成功。
  - 執行 `frontend` 目錄下 `npm run build` 成功。
  - 啟動 Spring Boot 後端與 Vite dev server。
  - 使用 in-app browser 驗證建立房間、加入房間、加入 AI、開始遊戲後 Mock AI 自動發言成功。
- Open items / next steps:
  - Phase 14 可開始 MVP review 與測試補強。
  - 瀏覽器 console 中仍可看到本輪修正前 HMR 產生的歷史 `createRoot` warning；修正後 build 成功，流程驗證成功。
  - 目前 Mock AI 行為是 deterministic MVP 版本，尚未接 LLM。
  - 自我檢查：AI action 經過 `PlayerActionService`；AI 未直接修改 `GameRoom` / `Vote` / `ChatMessage`；前端未揭露 AI/HUMAN；未導入登入、權限、DB 或 Redis。
