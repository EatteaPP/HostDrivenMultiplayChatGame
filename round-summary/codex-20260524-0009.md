# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 8 VOTING。
- Key changes:
  - 新增 `Vote` domain model 與 `VoteAcceptedView`。
  - 新增 `VoteService`，驗證並記錄 `SUBMIT_VOTE`。
  - 擴充 `PlayerActionService` 支援 `SUBMIT_VOTE`。
  - 擴充 `AvailableActionService`，在 `VOTING` 提供 `SEND_MESSAGE` 與 `SUBMIT_VOTE`，投票目標只包含活著且非自己的玩家。
  - 擴充 `GameFlowEngine`，支援 DISCUSSION timeout 後進入 `VOTING`。
  - 新增 `GameScheduler`，定期檢查 active rooms，但實際 stage transition 仍由 `GameFlowEngine` 執行。
  - 啟用 Spring scheduling。
  - 補充投票、availableActions 與 timeout transition 測試。
- Validation:
  - 執行 `mvn -q test` 成功。
- Open items / next steps:
  - Phase 9 可開始實作 ELIMINATION 與投票結算。
  - 目前 `VOTING` timeout 尚未結算投票，會在 Phase 9 處理。
  - 本輪尚未實作淘汰、GAME_ENDED、AI 或前端。
  - 自我檢查：投票必須經過 `PlayerActionService`；不可投自己、不可重複投、不可投已淘汰玩家；Scheduler 不直接修改 stage，轉場集中在 `GameFlowEngine`；未導入 DB / Redis / 登入 / 權限系統。
