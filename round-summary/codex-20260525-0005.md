# Round Summary

- Date: 2026-05-25
- Branch: unknown
- Goal: 新增可一鍵開啟前後端的 PowerShell 腳本，方便本機測試。
- Key changes:
  - 新增 `scripts/start-dev.ps1`：
    - 一次開兩個新的 PowerShell 視窗。
    - 視窗標題清楚標示：
      - `HostGame Backend (Spring Boot)`
      - `HostGame Frontend (Vite)`
    - Backend 視窗會設定 Java/Maven 環境後執行 `mvn spring-boot:run`。
    - Frontend 視窗會設定 NVM 環境後執行 `npm.cmd run dev -- --host 127.0.0.1 --port 5173`。
  - 更新 `README.md`，加入一鍵啟動腳本用法。
- Validation:
  - 手動檢查腳本內容與路徑設定正確，且 README 已加入啟動指令。
  - Validation not run: 本輪未在此環境直接啟動 GUI 視窗驗證（會開新 console）。
- Open items / next steps:
  - 若你希望，我可以再補一支 `scripts/stop-dev.ps1`，一鍵關閉 8080/5173 對應程序，方便反覆測試。
  - 自我檢查：本輪僅新增啟動腳本與文件，不影響遊戲規則、身份揭露與 action 驗證邊界。
