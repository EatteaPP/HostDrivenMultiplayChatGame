# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 3 WebSocket / STOMP 基礎事件。
- Key changes:
  - 新增 `WebSocketConfig`，提供 `/ws` STOMP endpoint、`/topic` public broker、`/user/queue/events` private queue。
  - 新增 `WsEvent` envelope 與 `WsEventType`。
  - 新增 `WebSocketBroadcaster`，集中處理房間廣播與 private event 發送。
  - 更新 `RoomController`，建立房間與加入房間後廣播 `ROOM_STATE_UPDATED`。
  - 新增 `WebSocketBroadcasterTest`，驗證 public topic 與 private queue 的目的地。
- Validation:
  - 執行 `mvn -q test` 成功。
- Open items / next steps:
  - Phase 4 可開始建立 `PlayerActionService` 與 `SEND_MESSAGE`。
  - 本輪尚未實作聊天、Host message、stage transition、AI 或前端。
  - 自我檢查：private event 使用 `/user/queue/events`，未廣播到全房間；`MESSAGE_CREATED` 與 `STAGE_CHANGED` 尚未混用；未導入 DB / Redis / 登入 / 權限系統。
