# Round Summary

- Date: 2026-05-25
- Branch: unknown
- Goal: 讓 MVP 更接近可實際運作，加入房主可調參數、最大輪次收斂、以及暫時「找出叛徒 / 找出 AI」目標支援。
- Key changes:
  - 後端新增 `RoomObjective`（`FIND_TRAITOR`、`FIND_AI`），`GameRoom` 與 `RoomView` 增加 objective 與 flow 設定欄位。
  - `CreateRoomRequest` 擴充房主可設定欄位：`discussionSeconds`、`votingSeconds`、`messageCooldownSeconds`、`maxRounds`、`objective`。
  - `GameFlowConfig` 新增 `maxRounds`（預設 10）。
  - `RoomService.createRoom` 改為接收 `CreateRoomRequest`，並驗證秒數/輪次需大於 0。
  - `GameFlowEngine`：
    - 開局依 objective 指派隱藏身份（MVP 版：`FIND_TRAITOR` 會在真人中指定 1 名叛徒；`FIND_AI` 以 AI 控制玩家作為叛徒）。
    - 淘汰後若 `alivePlayers <= 3` 進入結束。
    - 若輪次達 `maxRounds` 仍未結束，直接進入結束判定。
    - 勝負規則改為：只要任一叛徒存活即叛徒方勝，否則平民方勝。
  - 前端 Lobby 新增建立房間設定面板（目標、討論秒數、投票秒數、發言 CD、最大輪次），並在房間資訊顯示設定值與輪次上限。
  - 當 objective 不是 `FIND_AI` 時，前端會禁用 `Add AI` 按鈕。
  - 測試更新：`RoomServiceTest`、`DtoVisibilityTest`。
- Validation:
  - `mvn -q test`（已執行，通過）
  - `frontend: npm.cmd run build`（已執行，通過）
  - Markdown 檢查：以 UTF-8 讀取並掃描 `�`，未發現主要 `.md` 檔案包含 replacement char
- Open items / next steps:
  - 目前 `FIND_TRAITOR` 採 MVP 簡化指派策略（固定取第一位真人為叛徒）；若要更公平可改為可重現隨機（seeded random）。
  - 若要完全關閉 AI 相關流程，可在下一輪把 `/ai-players` API 與 `AgentCoordinator` 觸發改為 feature flag。
  - `frontend/dist` 已因 build 產生新檔名（hash 資產），是否保留版本化輸出需依 repo 策略確認。
  - 自我檢查：規則判定仍在後端；availableActions 由後端計算；身份未在遊戲結束前透過 public DTO 洩漏；玩家 action 仍經 `PlayerActionService`；未導入 DB/Redis/登入/權限系統。
