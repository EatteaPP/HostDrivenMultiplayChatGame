# Round Summary

- Date: 2026-05-25
- Branch: unknown
- Goal: 新增房間建立時可輸入「本輪要找出的叛徒描述」欄位，並在房間畫面顯示。
- Key changes:
  - 後端：
    - `GameRoom` 新增 `objectiveHint` 欄位。
    - `CreateRoomRequest` 新增 `objectiveHint`。
    - `RoomView` 新增 `objectiveHint` 回傳欄位。
    - `RoomService` 新增 `objectiveHint` 正規化與驗證（trim、空字串轉 null、長度上限 120）。
  - 前端：
    - 建立房間區塊新增文字輸入欄位「本輪辨識目標」。
    - 送出 `createRoom` 時將 `objectiveHint` 一併送後端。
    - 房間列表與房間詳情區塊顯示 `objectiveHint`（有值才顯示）。
  - 測試：
    - `RoomServiceTest` 更新 `CreateRoomRequest` 參數並驗證 `objectiveHint` 保存成功。
    - `MockAgentControllerTest` 更新建構參數相容性。
- Validation:
  - `mvn -q test`（已執行，通過）
  - `frontend: npm.cmd run build`（已執行，通過）
- Open items / next steps:
  - 目前 `objectiveHint` 屬於描述欄位，尚未參與自動規則判定；若後續要轉為可配置規則，建議另加結構化欄位避免字串解析。
  - 自我檢查：本輪未改動核心勝負與身份揭露邏輯；availableActions 與 PlayerActionService 邊界維持不變。
