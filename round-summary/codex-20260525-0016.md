# Round Summary

- Date: 2026-05-25
- Branch: unknown
- Goal: 實作「玩家離開頁面視為斷線淘汰」，並確保投票與行動選項自動排除該玩家。
- Key changes:
  - 後端：
    - `RoomService` 新增 `disconnectPlayer(roomId, playerId)`，將存活玩家標記為 `ELIMINATED`。
    - `RoomController` 新增 `POST /api/rooms/{roomId}/players/{playerId}/disconnect`。
    - 玩家斷線時由 Host 公告 `Player N disconnected and was eliminated.`。
    - 新增 `GameFlowEngine.handlePlayerAvailabilityChanged`，處理斷線後可能觸發的流程推進（投票全到齊或達結束門檻）。
  - 前端：
    - 新增 `beforeunload` / `pagehide` 事件，使用 `navigator.sendBeacon` 在離頁時呼叫 disconnect API。
    - 讓瀏覽器端意外離開後，房內其他玩家能收到狀態更新。
  - 測試：
    - `RoomServiceTest` 新增斷線標記為淘汰的單元測試。
- Validation:
  - `mvn -q test`（已執行，通過）
  - `frontend: npm.cmd run build`（已執行，通過）
- Open items / next steps:
  - 目前採「離頁即淘汰」收斂策略，後續若要支援回房可再做 reconnect/session restore。
  - `sendBeacon` 在特定瀏覽器情境仍可能漏送，若要更穩可再加 WebSocket session disconnect 監聽補強。
  - 自我檢查：斷線玩家會失去可用 action 並被投票目標排除，規則仍由後端驗證。
