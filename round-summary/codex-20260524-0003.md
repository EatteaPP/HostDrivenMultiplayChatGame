# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 2 Lobby / Room API。
- Key changes:
  - 新增 `RoomService`，支援建立房間、查詢房間、取得房間、加入等待中房間。
  - 新增 `RoomController`，提供 `POST /api/rooms`、`GET /api/rooms`、`POST /api/rooms/{roomId}/join`、`GET /api/rooms/{roomId}`。
  - 新增 API DTO：`CreateRoomRequest`、`JoinRoomRequest`、`JoinRoomResponse`、`RoomView`。
  - 新增 `ApiExceptionHandler`，處理未知房間與不可加入房間。
  - 新增 `RoomServiceTest`，驗證房間建立、加入房間、非等待中房間拒絕加入、未知房間拒絕。
- Validation:
  - 執行 `mvn -q test` 成功。
- Open items / next steps:
  - Phase 3 可開始建立 WebSocket / STOMP 基礎事件。
  - 本輪尚未實作 WebSocket、stage transition、PlayerActionService、AI 或前端。
  - 自我檢查：Room API 回傳 `RoomView` / `PlayerPublicView`，未直接回傳 domain internal state；未導入 DB / Redis / 登入 / 權限系統；Host 未被加入 players list。
