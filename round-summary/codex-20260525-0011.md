# Round Summary

- Date: 2026-05-25
- Branch: unknown
- Goal: 讓房間在建立時可設定「開局最少人數」與「結束存活門檻」。
- Key changes:
  - 後端新增房間規則參數：
    - `minPlayersToStart`（預設 4）
    - `endWhenAlivePlayersLE`（預設 3）
  - `CreateRoomRequest` / `RoomView` 擴充上述欄位，支援建立與回傳。
  - `RoomService` 新增驗證：
    - `minPlayersToStart >= 2`
    - `endWhenAlivePlayersLE >= 1`
    - `endWhenAlivePlayersLE < minPlayersToStart`
  - `GameFlowEngine` 規則改為使用設定值：
    - 開局需滿足 `players.size >= minPlayersToStart`
    - 結束條件改為 `alivePlayers <= endWhenAlivePlayersLE`
  - 前端建立房間區塊新增兩個輸入欄位，並在房間資訊區顯示目前設定值。
  - 前端 `Start` 按鈕改為未達最少人數時禁用。
  - 測試同步更新（`GameFlowEngineTest`、`RoomServiceTest`、`MockAgentControllerTest`）。
- Validation:
  - `mvn -q test`（已執行，通過）
  - `frontend: npm.cmd run build`（已執行，通過）
- Open items / next steps:
  - 後續可加前端提示訊息（例如「還差 N 人才能開始」）來提升可用性。
  - 自我檢查：本輪規則仍由後端驗證與執行，未新增身份洩漏風險與未要求的外部依賴。
