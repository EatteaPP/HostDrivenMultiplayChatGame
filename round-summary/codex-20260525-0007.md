# Round Summary

- Date: 2026-05-25
- Branch: unknown
- Goal: 修正 `start-dev.ps1` 前端視窗啟動時的命令字串被拆解問題。
- Key changes:
  - `scripts/start-dev.ps1` 改為使用 `-EncodedCommand` 啟動新 PowerShell 視窗，避免 `-Command` 字串轉義/引號破壞。
  - 以 single-quoted here-string 建立命令模板，避免 `$env`、`$host` 在主腳本階段被提早展開。
  - 透過 placeholder (`__BACKEND_CWD__`, `__FRONTEND_CWD__`) 代入實際路徑，確保 `Set-Location -LiteralPath` 可正確執行。
- Validation:
  - 使用 PowerShell Parser 進行語法檢查，結果 `PARSE_OK`。
  - Validation not run: 本輪未在此環境直接開 GUI 視窗驗證（會啟動新 console）。
- Open items / next steps:
  - 請在本機重跑 `powershell -ExecutionPolicy Bypass -File .\scripts\start-dev.ps1`，確認兩個視窗可正常啟動。
  - 若仍異常，下一步改為產生暫存 `.ps1` 檔並以 `-File` 啟動，可再降低命令列轉義風險。
  - 自我檢查：本輪僅修正啟動腳本，不影響遊戲邏輯與規則邊界。
