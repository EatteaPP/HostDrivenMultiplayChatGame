# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 6 stage 與 `startGame`。
- Key changes:
  - 新增 `GameFlowEngine`，支援 `WAITING -> DISCUSSION`。
  - 新增 `POST /api/rooms/{roomId}/start`。
  - 新增 `AvailableActionService`，由後端計算玩家可用 action。
  - 新增 `StageChangedView` 與 `AvailableActionsUpdatedView`。
  - `startGame` 會設定 `roomStatus`、`currentStage`、`startedAt`、`stageStartedAt`、`stageEndsAt`、`round`。
  - `startGame` 會產生 HOST 開場訊息、廣播 `STAGE_CHANGED`、發送 private `AVAILABLE_ACTIONS_UPDATED`、廣播 `ROOM_STATE_UPDATED`。
  - 新增 `GameFlowEngineTest` 與 `AvailableActionServiceTest`。
- Validation:
  - 執行 `mvn -q test` 成功。
- Open items / next steps:
  - Phase 7 可開始實作 cooldown。
  - `sendPrivateEvent` 目前以 `playerId` 作為 STOMP user destination 名稱；未導入登入前這是 MVP placeholder，後續前端串接時需確認 session/user mapping。
  - 本輪尚未實作 scheduler timeout、投票、淘汰、AI 或前端。
  - 自我檢查：availableActions 由後端計算；`MESSAGE_CREATED` 與 `STAGE_CHANGED` 分開；HOST 未放入 players list；未導入 DB / Redis / 登入 / 權限系統。
