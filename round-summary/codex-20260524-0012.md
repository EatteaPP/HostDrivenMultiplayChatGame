# Round Summary

- Date: 2026-05-24
- Branch: master
- Goal: 實作 Phase 11 React Lobby。
- Key changes:
  - 使用 nvm-windows 安裝並啟用 Node 22.22.3。
  - 新增 `frontend/` Vite + React + TypeScript 專案。
  - 新增 Lobby UI，支援刷新等待中房間、建立房間、加入房間。
  - 串接後端 Room API：`GET /api/rooms?status=WAITING`、`POST /api/rooms`、`POST /api/rooms/{roomId}/join`。
  - 新增 Vite proxy，將 `/api` 代理到 `http://127.0.0.1:8080`。
  - 新增前端 responsive layout 與基本狀態顯示。
- Validation:
  - 執行 `frontend` 目錄下 `npm install` 成功，npm audit 顯示 0 vulnerabilities。
  - 執行 `frontend` 目錄下 `npm run build` 成功。
  - 執行後端 `mvn -q test` 成功。
  - 確認主要新增前端文字檔為 UTF-8 without BOM。
- Open items / next steps:
  - Phase 12 可開始建立 React Game Room。
  - 目前 Lobby 尚未接 WebSocket，房間列表靠手動 refresh。
  - 目前前端使用 `npm.cmd`，因 PowerShell execution policy 會阻擋 `npm.ps1`。
  - 自我檢查：前端只顯示後端 public DTO；未顯示 AI/HUMAN 身份；未在前端實作遊戲規則；未導入登入、權限、DB 或 Redis。
